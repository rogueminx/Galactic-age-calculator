export class Fight {
  constructor(wizard, necro) {
    this.wizard = wizard;
    this.necro = necro;
    this.turn = true; // if true, wizard turn; if false, necro
  }

  strike() {

  } // strike

  
} //Fight
